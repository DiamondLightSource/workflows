apiVersion: argoproj.io/v1alpha1
kind: ClusterWorkflowTemplate
metadata:
  name: python-hello

spec:
  entrypoint: main

  templates:
    - name: main
      container:
        image: alpine:3.19
        command: [sh, -c]
        args:
          - echo hello
