{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "common.names.fullname" $ }}
  namespace: {{ .Release.Namespace }}
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
  labels:
    {{- include "common.labels.standard" $ | nindent 4 }}
spec:
  ingressClassName: nginx
  rules:
    - host: kubernetes-dashboard.workflows.diamond.ac.uk
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "common.names.dependency.fullname" (dict "chartName" "vcluster" "chartValues" (index .Values "kubernetes-dashboard") "context" $) }}-server
                port:
                  name: https
  tls:
    - secretName: {{ include "common.names.dependency.fullname" (dict "chartName" "vcluster" "chartValues" (index .Values "kubernetes-dashboard") "context" $) }}-ingress-tls
      hosts:
        - kubernetes-dashboard.workflows.diamond.ac.uk
{{- end }}
