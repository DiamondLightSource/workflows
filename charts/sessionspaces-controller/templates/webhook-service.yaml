{{- if and $.Values.webhook.enabled $.Values.webhook.service.create }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "sessionspaces-controller.webhook.fullname" $ }}
  namespace: {{ .Release.Namespace }}
  {{- with $.Values.webhook.service.annotations }}
  annotations:
    {{- . | toYaml | nindent 4 }}
  {{- end }}
  labels:
    {{- include "sessionspaces-controller.webhook.labels" $ | nindent 4 }}
spec:
  type: {{ $.Values.webhook.service.type }}
  selector:
    {{- include "sessionspaces-controller.webhook.matchLabels" $ | nindent 4 }}
  ports:
    - name: http
      port: {{ $.Values.webhook.service.port }}
      targetPort: http
      protocol: TCP
{{- end }}
