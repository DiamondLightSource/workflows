argocd:
  enabled: true
  targetRevision: HEAD
  extraValueFiles:
    - staging-values.yaml

events:
  enabled: false
  extraValueFiles:
    - staging-values.yaml

groups:
  enabled: false
  targetRevision: HEAD

kyverno:
  enabled: false
  valuesObject:
    admissionController:
      replicas: 1
      container:
        resources:
          limits:
            cpu: 1
            memory: 8Gi
          requests:
            cpu: 100m
            memory: 1Gi
    backgroundController:
      extraArgs:
        genWorkers: 10
      replicas: 1
      resources:
        limits:
          cpu: 1
          memory: 8Gi
        requests:
          cpu: 100m
          memory: 4Gi

sessionspaces:
  enabled: false
  targetRevision: HEAD
  extraValueFiles:
    - staging-values.yaml

sealedsecrets:
  enabled: false
  valuesObject:
    resources:
      limits:
        cpu: 200m
        memory: 512Mi
      requests:
        cpu: 100m
        memory: 256Mi

workflows:
  enabled: false
  targetRevision: HEAD
  extraValueFiles:
    - staging-values.yaml

graphProxy:
  enabled: false
  targetRevision: HEAD
  extraValueFiles:
    - staging-values.yaml

dashboard:
  enabled: false
  targetRevision: HEAD
  extraValueFiles:
    - staging-values.yaml

otel:
  enabled: false
  targetRevision: HEAD
  extraValuesFiles: []
  valuesObject: {}
