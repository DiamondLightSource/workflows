argo-workflows:
  enabled: true
  crds:
    install: false
  singleNamespace: true
  createAggregateRoles: false
  controller:
    clusterWorkflowTemplates:
      enabled: false
  server:
    clusterWorkflowTemplates:
      enabled: false
    authModes: ["server"]
    ingress:
      enabled: true
      hosts:
        - workflows.diamond.ac.uk
  workflow:
    serviceAccount:
      create: true

