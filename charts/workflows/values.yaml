argo-workflows:
  enabled: true
  controller:
    workflowDefaults:
      spec:
        serviceAccountName: argo-workflow
  server:
    authModes: ["server"]
    sso:
      enabled: false
      issuer: https://authbeta.diamond.ac.uk/cas/oidc
  workflow:
    serviceAccount:
      create: true
