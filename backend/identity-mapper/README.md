# identity-mapper

This is an MVP to collect user group information to patch Argo Worflows and Pod securityContext based on LDAP information.

TODO: if this is successful, replace with a rust implementation.